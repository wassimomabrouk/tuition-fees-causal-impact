# Verified working together on Python 3.12+.
duckdb
pandas
numpy
pyarrow
scipy
matplotlib
statsmodels
xlrd                # legacy .xls reader, for the Fachserie volumes (phase 2)
openpyxl            # .xlsx reader, for Fachserie volumes from WS 2013/14 (phase 3)
# Causal / staggered DiD:
differences>=0.3.0
pyfixest>=0.60.0
wildboottest        # wild cluster bootstrap, few-cluster inference
# Pipeline tests:
pytest
